{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "crons": [
    {
      "path": "/api/stocks/refresh",
      "schedule": "*/15 * * * *"
    },
    {
      "path": "/api/crypto/refresh",
      "schedule": "*/15 * * * *"
    }
  ]
}
